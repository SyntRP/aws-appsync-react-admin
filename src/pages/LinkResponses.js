import LinkResponses from "../components/link-responses";
import withSuspense from "../helpers/hoc/withSuspense";

const LinkResponsesPage = () => {
  return <LinkResponses />;
};

export default withSuspense(LinkResponsesPage);
