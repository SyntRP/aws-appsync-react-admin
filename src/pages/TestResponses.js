import TestResponses from "../components/test-responses";
import withSuspense from "../helpers/hoc/withSuspense";

const TestResponsesPage = () => {
  return <TestResponses />;
};
export default withSuspense(TestResponsesPage);
